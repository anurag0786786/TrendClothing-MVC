@page
@model LoginModel
@{
    ViewData["Title"] = "Login";
}

@section Styles {
    <link rel="stylesheet" href="~/css/tc-theme.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/auth.css" asp-append-version="true" />
}

<div class="tc-auth-page">

    <!-- LEFT BRAND PANEL -->
    <div class="tc-auth-left">
        <a href="/" class="tc-auth-left__brand">TrendClothing</a>

        <div class="tc-auth-left__body">
            <p class="tc-auth-left__eyebrow">New Season Drop</p>
            <h1 class="tc-auth-left__title">Welcome<br />Back.</h1>
            <p class="tc-auth-left__sub">Login and explore the latest fashion trends crafted for every style.</p>

            <a asp-page="./Register"
               asp-route-returnUrl="@Model.ReturnUrl"
               class="tc-auth-left__action">
                Create Account →
            </a>

            <div class="tc-auth-left__social">
                <p class="tc-auth-left__social-label">Or sign in with</p>
                <form asp-area="Identity"
                      asp-page="/Account/ExternalLogin"
                      asp-route-returnUrl="@Model.ReturnUrl"
                      method="post">
                    <button type="submit" name="provider" value="Google" class="tc-auth-social-btn">
                        <img src="~/Images/google.svg" alt="Google" />
                        Continue with Google
                    </button>
                </form>
                <form asp-area="Identity"
                      asp-page="/Account/ExternalLogin"
                      asp-route-returnUrl="@Model.ReturnUrl"
                      method="post">
                    <button type="submit" name="provider" value="Facebook" class="tc-auth-social-btn">
                        <img src="~/Images/facebook.svg" alt="Facebook" />
                        Continue with Facebook
                    </button>
                </form>
            </div>
        </div>

        <div style="font-size:12px; color:rgba(255,255,255,0.25);">© TrendClothing</div>
    </div>

    <!-- RIGHT FORM PANEL -->
    <div class="tc-auth-right">
        <div class="tc-auth-form-wrap">

            <h2>Sign In</h2>
            <p class="tc-auth-sub">Enter your credentials to continue</p>

            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger" style="margin-bottom:16px; font-size:13px;"></div>

                <div class="tc-auth-field">
                    <input asp-for="Input.Email"
                           placeholder=" "
                           autocomplete="username"
                           required />
                    <label>Email address</label>
                    <span asp-validation-for="Input.Email" class="tc-auth-error"></span>
                </div>

                <div class="tc-auth-field">
                    <input asp-for="Input.Password"
                           type="password"
                           placeholder=" "
                           autocomplete="current-password"
                           required />
                    <label>Password</label>
                    <span asp-validation-for="Input.Password" class="tc-auth-error"></span>
                </div>

                <div class="tc-auth-extra">
                    <label>
                        <input asp-for="Input.RememberMe" />
                        Remember me
                    </label>
                    <a asp-page="./ForgotPassword">Forgot password?</a>
                </div>

                <button type="submit" class="tc-auth-submit">Login</button>

                <div class="tc-auth-footer-link">
                    <a asp-page="./ResendEmailConfirmation">Resend confirmation email</a>
                </div>
            </form>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
